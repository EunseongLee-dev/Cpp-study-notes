# C++ 상속 정리 및 복습 메모

## 1. 객체지향 기본 특징
1. **캡슐화(Encapsulation)**: 데이터 은닉 및 접근 제한
2. **상속(Inheritance)**: 부모 클래스의 멤버를 자식 클래스에서 재사용
3. **다형성(Polymorphism)**: 동일한 인터페이스로 다른 동작 수행
4. **추상화(Abstraction)**: 구현을 숨기고 인터페이스 제공

## 2. 접근 지정자
| 지정자 | 부모 클래스에서 접근 | 외부에서 접근 | 자식에서 접근 |
|--------|-----------------|------------|------------|
| private | X | X | X |
| protected | X | X | O |
| public | O | O | O |

- private: 상속받은 자식도 접근 불가
- protected: 자식에서 접근 가능, 외부는 불가
- public: 외부와 자식 모두 접근 가능

## 3. 상속 선언 방식
```cpp
class Child : public Parent {};
```
- public 상속이 일반적.  
- protected/private 상속은 제한적 사용, 대부분 public으로 선언.

## 4. 생성자 호출과 초기화
- **생성자 호출 순서**: 부모 -> 자식
- **멤버 초기화 리스트**: `Parent(int a) : m_i(a) {}`  
  - 중괄호 `{}` 내부는 생성 후 대입  
  - `:` 초기화 리스트는 생성과 동시에 초기화 → 효율적, const 멤버나 참조형 멤버 초기화 가능
- **생성자 오버로딩**: 기본 생성자 외 인자 받는 생성자는 초기화 리스트로 호출

## 5. private/protected/public 멤버 사용
- 자식 클래스 생성자/멤버 함수에서 부모 멤버 접근 가능 여부:
  - private → 접근 불가
  - protected → 접근 가능
  - public → 접근 가능
- 외부에서는 private/protected 멤버 접근 불가

## 6. 메모리 구조
- 자식 클래스 객체 = 부모 클래스 멤버 + 자식 멤버  
- 부모 멤버와 자식 멤버가 **연속된 메모리 영역**에 할당됨  
- 부모 객체를 따로 만들지 않아도, 자식 객체가 부모 부분을 포함

## 7. virtual 키워드와 다형성
- 부모 클래스에서 `virtual` 함수 선언 → 자식에서 오버라이드 가능
- 포인터나 참조를 부모 타입으로 사용 시, 실제 객체 타입에 맞는 함수 호출
- 사용 예:
```cpp
Character* c = new Warrior(...);
c->Attack(); // Warrior::Attack 호출
```
- virtual 없으면 부모 클래스 기준 호출

## 8. 주의/햇갈리기 쉬운 부분
1. **초기화 리스트 vs 중괄호 대입**
   - 초기화 리스트: 멤버 생성 시 바로 초기화
   - 중괄호 내부 대입: 객체 생성 후 값 대입
2. **상속된 private 멤버**
   - 자식에서 접근 불가
3. **virtual의 필요성**
   - 포인터/참조를 부모 타입으로 다루고 실제 객체의 동작을 호출할 때 필요
4. **동적 할당 vs 기존 객체 사용**
   - `new` → 새로운 객체 생성  
   - `&객체` → 기존 객체 주소를 부모 포인터로 관리

## 9. 핵심 포인트 요약
- 상속 구조에서 **부모 객체가 아닌 자식 객체가 실제 메모리를 포함**한다는 점
- **virtual + 포인터/참조**로 다형성 구현
- 초기화 리스트 사용 습관 → 효율적 초기화
- 접근 지정자에 따른 접근 가능 범위 명확히 인지
- 부모-자식 관계 이해 → 메모리, 호출 순서, 접근 제한 체감
