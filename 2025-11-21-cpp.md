# C++ 상속 및 오버라이딩 정리

## 1. 상속(Inheritance)
- **목적:** 코드 재사용, 계층 구조 표현, 기능 확장
- **접근 지정자:**
  - `public`: 외부 접근 가능 + 상속 시 자식 클래스에서도 접근 가능
  - `protected`: 외부 접근 불가, 상속 시 자식 클래스에서 접근 가능
  - `private`: 외부 접근 불가, 상속 시 자식 클래스에서도 접근 불가

- **생성자 호출 순서:** 부모 -> 자식
- **소멸자 호출 순서:** 자식 -> 부모
- **메모리 배치:** 자식 객체는 부모의 멤버를 포함함 (메모리 상에서 연속 배치)

## 2. 오버라이딩(Overriding)
- **정의:** 부모 클래스의 함수와 동일한 시그니처를 가진 함수를 자식 클래스에서 재정의
- **조건:**
  - 함수명이 동일해야 함
  - 매개변수 개수, 타입도 동일해야 함
- **사용 이유:** 특정 자식 클래스에서 부모 기능 대신 독자 기능을 구현

```cpp
class Parent {
public:
    virtual void Run() { std::cout << "Parent Run" << std::endl; }
};
class Child : public Parent {
public:
    void Run() override { std::cout << "Child Run" << std::endl; }
};
```

- `override` 키워드
  - 선택적이지만, 부모 함수와 정확히 일치하지 않으면 컴파일 오류 발생
  - 실수를 방지하고 코드 명확성을 높임

## 3. 가상함수(Virtual Function)
- **정의:** 부모 포인터를 통해 자식 객체를 가리킬 때, 자식의 재정의된 함수가 실행되도록 함
- **정적 바인딩 vs 동적 바인딩**
  - **정적 바인딩(Static Binding):** 컴파일 시점에 호출될 함수 결정 (기본 함수)
  - **동적 바인딩(Dynamic Binding):** 런타임 시점에 실제 객체 타입에 따라 호출 함수 결정 (virtual 함수)

```cpp
Parent* p = new Child();
p->Run(); // Child의 Run 실행
```

- virtual 없으면 정적 바인딩 적용 → 부모 함수 호출
- virtual 있으면 동적 바인딩 적용 → 실제 객체(Child) 함수 호출

## 4. 생성자 초기화 리스트 vs 대입
- **초기화 리스트 사용 이유:**
  - 상속 시 부모 생성자 호출
  - const, reference 멤버 초기화
  - 성능 최적화

```cpp
Child(int x) : Parent(x), m_value(0) {}
```
- `{}` 안에서 대입 가능하지만, 초기화 리스트는 직접 생성과 동시에 초기화

## 5. 실습 포인트
- 부모 클래스 포인터로 여러 자식 객체 배열을 관리하고, `virtual` 함수로 반복 호출해보기
- 접근 지정자별 멤버 접근 가능 여부 확인
- 생성자 초기화 리스트와 중괄호 대입 차이 실험
- override 키워드 유무에 따른 컴파일/실행 결과 비교

## 6. 햇갈리기 쉬운 부분
- virtual 없을 때 부모 포인터 호출 시 오버라이딩 안됨
- override 없이도 실행은 가능하지만, 함수 시그니처 틀리면 다른 함수 호출됨
- private 멤버는 자식 클래스에서 접근 불가
- 컴파일 시점(정적 바인딩) vs 런타임 시점(동적 바인딩) 개념
- 생성자 순서와 소멸자 순서 (부모 -> 자식 / 자식 -> 부모)

