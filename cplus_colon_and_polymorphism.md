# C++ 오늘 학습 복습 정리 (다형성 & 콜론(:) 사용)

## 1. 다형성(Polymorphism)

### 핵심 개념
- 부모 클래스 포인터를 통해 여러 자식 객체를 참조할 수 있음
- 부모 클래스의 가상 함수(virtual) 사용 시, 자식 클래스의 오버라이딩된 함수가 호출됨
- 다형성의 목적: 코드 재사용, 유연성, 확장성

### 주요 사항
1. 부모 포인터로 자식 객체 참조 가능, 자식 포인터로 부모 객체는 불가
2. virtual 함수: 부모 포인터가 자식 객체를 참조해도 자식의 함수가 실행됨
3. dynamic_cast: 부모 포인터가 실제로 자식 객체를 가리키는지 런타임에 확인
   - 성공: 자식 포인터 생성 가능
   - 실패: nullptr 반환
   - 용도: 부모 포인터로 자식의 고유 함수 안전하게 호출

### 주의점
- 단순히 자식 클래스 함수 호출만 원할 때는 자식 객체를 직접 생성해도 됨
- dynamic_cast는 런타임 안전성을 위해 사용, 항상 필요하지는 않음
- 가상 함수가 부모에 없으면 부모 포인터로 자식 함수 호출 불가

### 순수 가상 함수(pure virtual function)
- `virtual void func() = 0;` 형식
- 의미: 자식 클래스에서 반드시 구현해야 함
- 부모 클래스에 구현 {}가 있으면 자식에서 구현 안해도 기본 동작 사용 가능
- 용도: 인터페이스 역할, 상속 구조에서 강제 구현

---

## 2. 콜론(:) 사용 정리

### 1) 생성자 초기화 리스트
```cpp
class Monster {
    std::string type;
    int hp;
public:
    Monster(const std::string& t, int h) : type(t), hp(h) {}
};
```
- 멤버 변수 또는 부모 클래스 초기화
- const, 참조형 변수, 상속 시 필수

### 2) 상속 초기화
```cpp
class Player : public Character {
    std::string name;
public:
    Player(const std::string& n, int h) : Character(h), name(n) {}
};
```
- 부모 클래스 생성자를 호출하면서 멤버 초기화

### 3) 범위 지정 연산자 (::)
```cpp
std::cout << "Hello";
```
- 네임스페이스나 클래스 범위 명시

### 4) 삼항 연산자 (?:)
```cpp
int a = 10;
int b = (a > 5) ? 100 : 0;
```
- 조건식이 참이면 `?` 뒤 값, 거짓이면 `:` 뒤 값

### 5) 범위 기반 for문
```cpp
int arr[3] = {10,20,30};
for(int n : arr) {
    std::cout << n << "\n";
}
```
- 배열, vector, list 등 반복 가능한 컨테이너 순회
- 참조(&) 사용 시 원본 요소 수정 가능

### 6) 레이블
```cpp
start:
std::cout << "Hello";
```
- goto 레이블에 사용

---

## 3. 오늘 학습 내용 복습 포인트

1. 다형성의 이해와 필요성
   - 부모 포인터, virtual 함수, dynamic_cast 개념 확실히 이해
   - 순수 가상 함수 vs 기본 구현

2. dynamic_cast 사용 사례와 제한
   - 부모 포인터가 실제 자식 객체일 때 안전하게 고유 함수 호출
   - 실패 시 nullptr 처리 및 조건문 활용

3. 콜론(:)의 다양한 용례
   - 생성자 초기화, 상속, 범위 지정, 삼항 연산, 범위 기반 for, 레이블
   - 문맥에 따라 의미가 달라진다는 점 숙지

4. 범위 기반 for문 익숙해지기
   - 배열, STL 컨테이너 모두 사용 가능
   - 참조(&)로 원본 수정, 값으로 복사 선택

5. 복습용 연습
   - 다양한 컨테이너에 범위 기반 for문 적용
   - 부모 포인터로 자식 함수 호출 실습
   - dynamic_cast 실패와 성공 케이스 모두 실습