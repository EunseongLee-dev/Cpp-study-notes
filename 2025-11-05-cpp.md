# C++ 수업 정리 (GetMax 함수 & assert 이해)

## 📘 오늘 학습한 주요 내용

### 1. GetAverage() 함수

```cpp
double CArr::GetAverage() const
{
    if (m_icount == 0) return 0;

    double tot = 0;
    for (int i = 0; i < m_icount; ++i)
        tot += m_pint[i];

    return tot / m_icount;
}
```

- 총합을 double로 계산 → 정수/실수 연산 모두 정확하게 표현 가능.

- int형 데이터를 double형 변수에 더하면 자동으로 형 변환(promotion)되어 문제 없음.

### 2. ShowAll() 함수

```cpp
void CArr::ShowAll()
{
    if (m_icount == 0 || m_pint == nullptr)
    {
        std::cout << "데이터 없음\\n";
        return;
    }

    std::cout << "입력된 데이터: ";
    for (int i = 0; i < m_icount; ++i)
    {
        std::cout << m_pint[i];
        if (i < m_icount - 1) std::cout << ", ";
    }
    std::cout << '\\n';
}
```

-마지막 쉼표 방지용 조건문 (i < m_icount - 1) 활용.

-빈 배열 상태에서의 안전 처리 포함.

### 3. `GetMax()` 함수 구현 과정

- 배열에서 **최대값을 찾는 함수**를 직접 구현함.  
- 처음 시도했던 방식:

```cpp
int CArr::GetMax() const
{
    if (m_icount == 0 || m_pint == nullptr)
    {
        return 0;
    }
    
    int max = 0;
    for (int i = 0; i < m_icount; ++i)
    {
        if (m_pint[i] < m_pint[i + 1])
        {
            max = m_pint[i + 1];
        }
        else 
        {
            max = m_pint[i];
        }
    }
    return max;
}
```

🧩 **문제점**  
- 인덱스 i와 i+1만 비교하기 때문에, 중간에서 큰 값이 나와도 이후 반복에서 덮어쓰기 됨.  
- 결국 **마지막 비교 결과만 반영**되어 정확한 최대값을 찾을 수 없음.

✅ **수정된 올바른 방식:**

```cpp
int max = m_pint[0];

for (int i = 0; i < m_icount; ++i)
{
    if (m_pint[i] > max)
    {
        max = m_pint[i];
    }
}
return max;
```

- 처음 요소를 기준으로 두고, 모든 요소를 순회하며 `max`를 갱신.
- 즉, **"지금까지 본 값 중 가장 큰 값"** 을 저장해가는 방식.

---

### 2. `assert()`의 의미

- 헤더: `#include <assert.h>`  
- 사용 예시: `assert(b != 0);`

#### 🧠 개념
- `assert()`는 **프로그램의 가정이 깨질 경우 강제 종료시키는 디버깅용 함수**.  
- 괄호 안의 조건이 `false`가 되면 프로그램이 종료된다.

#### 예시 해석
```cpp
assert(b != 0);
```
- 의미: “b는 0이 아니어야 한다.”  
- 만약 `b`가 0이라면 → `b != 0`은 `false` → 프로그램 **즉시 종료**.  
- 즉, **b가 0일 경우 실행 중단**이라는 뜻이다.

🧩 흔히 `assert(nullptr)`처럼 사용하면, **nullptr이면 프로그램을 종료**시키는 역할.

---

### 3. 헷갈렸던 부분 / 주의점

| 항목 | 내용 | 정리 포인트 |
|------|------|--------------|
| `!=` 연산자 | "같지 않다" | `b != 0` → b가 0이 아닐 때 참 |
| `assert()` | 조건이 거짓이면 종료 | 디버그 시 잘못된 값 감지용 |
| `for` 순회 시 비교 | i, i+1 방식은 덮어쓰기 문제 발생 | `max`를 따로 두고 갱신하는 방식 사용 |
| `GetMax` 설계 | 초기값 설정 중요 | 보통 `int max = m_pint[0];`로 시작 |

---

### 4. 추가로 구현해볼 수 있는 함수 아이디어

| 함수 이름 | 설명 |
|------------|------|
| `GetMin()` | 배열의 최소값 반환 |
| `GetSum()` | 배열의 합계 반환 |
| `GetAvg()` | 평균값 계산 (정수형 주의) |
| `Find(int value)` | 특정 값의 존재 여부 확인 |

이런 함수들은 `GetMax()`와 거의 동일한 구조로, **반복문과 비교 조건만 달라지는 응용 문제**로 적합하다.

---

### 5. 오늘 학습 키워드 요약

- **배열 순회 / 조건문 비교 / 변수 갱신 구조**
- **assert()를 통한 디버깅 안정성**
- **로직 검증 과정에서 인덱스 관계 실수 파악**

---

📌 **핵심 정리 문장:**  
> "for문 안에서 최대값을 구할 땐, 항상 기준값 하나를 잡고 그보다 크면 갱신하는 방식으로 접근해야 한다."

---
