## C++ Class 학습 정리 

### 1. 오늘 배운 Class 기초 내용

- **Class 정의**: 사용자 정의 자료형으로, 변수와 함수를 묶어 객체를 설계할 수 있음.
- **접근 지정자**:
  - `private`: 외부에서 접근 불가, 클래스 내부에서만 사용 가능.
  - `public`: 외부에서도 접근 가능.
  - **기본 접근 지정자**: class는 기본 `private`, struct는 기본 `public`.
- **생성자(Constructor)**:
  - 객체가 생성될 때 자동으로 호출.
  - 초기값 세팅 목적.
  - 초기화 리스트(`:`) 사용 가능.
- **소멸자(Destructor)**:
  - 객체가 소멸될 때 자동으로 호출.
  - 동적할당한 메모리를 해제할 때 사용.
  - 일반 변수만 가지고 있으면 굳이 작성 안 해도 됨.
- **멤버함수**: 해당 클래스가 사용하는 전용 함수.
- **포인터와 참조**:
  - 포인터(`*`): 객체 주소를 담는 변수, nullptr 가능, null 접근 시 위험.
  - 참조(`&`): 객체의 별칭, null 불가, 안전함.
- **동적할당**: `new`로 객체를 힙에 생성, `delete`로 해제.

### 2. 오늘 문제풀이 정리

#### 문제1: 평균 구하기 (동적할당 배열)
- **핵심 실수**: 마지막 평균 계산에서 `tot / num =`처럼 작성함 → C++에서는 왼쪽 변수 = 오른쪽 계산식으로 작성해야 함.
- **정리**:
```cpp
double avg = tot / num;
```
- **개념 체득 포인트**:
  - 동적할당 배열 사용 시, 크기를 미리 받아야 하고, 힙에 생성됨.
  - 평균 계산 시 왼쪽에는 결과를 담을 변수, 오른쪽에는 계산식.

#### 문제2: Player 클래스 (객체와 포인터/참조 혼동)
- **핵심 실수**:
  - `Player* a;` 초기화 안 하고 사용 → 초기화되지 않은 포인터 접근 오류.
  - 생성자가 `private` 상태라서 main에서 객체 생성 불가.
  - `Attack` 함수에서 포인터(`*`)와 참조(`&`) 혼용 시 헷갈림.
- **정리**:
  - 포인터 변수는 실제 객체가 아니라 객체 주소를 담는 상자임.
  - 객체를 가리킬 주소가 없으면 nullptr로 초기화하고, 사용 전 체크 필요.
  - 참조(`&`)는 항상 존재하는 객체 필요, 안전.
  - 접근 지정자는 선언 위치에 따라 적용됨.

### 3. 실수 및 헷갈린 점 정리

1. 생성자와 소멸자 기본 개념과 역할 헷갈림 → 생성자는 초기값 세팅, 소멸자는 동적 메모리 해제용.
2. 포인터(`*`)와 참조(`&`) 혼동 → 포인터는 주소 담는 상자, 참조는 안전한 별칭.
3. 동적할당 배열 문제 → 배열 생성 후 반드시 힙에 존재하는지 확인, 평균 계산식 작성법.
4. 접근 지정자 이해 → `public:` 선언 이후 코드만 public, 기본은 private.

### 4. 다음 복습/응용 포인트

- 클래스 객체 생성 시 포인터와 참조 안전하게 쓰는 방법 반복 학습.
- 동적할당 배열 문제 다시 풀어보며 left=right 구조 이해.
- Attack 함수 포인터 vs 참조 버전 만들어 실행해보기.
