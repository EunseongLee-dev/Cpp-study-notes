# C++ 문자, 문자열 및 포인터 정리

## 1. 문자 및 문자열

### 문자 타입
- `char`: 1바이트, ASCII 문자 저장.
- `wchar_t`: 와이드 문자, 운영체제 및 컴파일러에 따라 2~4바이트, 주로 유니코드 문자 저장.
- `char16_t`: 2바이트 고정, UTF-16 문자 저장, 다양한 언어 지원 가능.
- `char32_t`: 4바이트 고정, UTF-32 문자 저장, 거의 모든 유니코드 문자를 1개 단위로 표현 가능.

### 사용 예시
```cpp
char c = 'A';
wchar_t wc = L'가';
char16_t c16 = u'한';
char32_t c32 = U'😀';
```
- 접두사:
  - `L' '` → `wchar_t`
  - `u' '` → `char16_t`
  - `U' '` → `char32_t`

### 문자열
- `std::string`: char 기반 문자열.
- `std::wstring`: wchar_t 기반 문자열.
- `std::u16string`: char16_t 기반 문자열.
- `std::u32string`: char32_t 기반 문자열.

```cpp
std::string str = "Hello";
std::wstring wstr = L"안녕";
std::u16string u16str = u"한글";
std::u32string u32str = U"😀";
```

> char16_t/char32_t는 다양한 언어와 이모지를 지원할 때 주로 사용됨. 일반적인 영어 문자열은 거의 사용하지 않음.

## 2. 포인터와 2중 포인터 정리

### 단일 포인터
- 구조체를 가리킬 때: `Player* pPlayer`
- 사용 예시: `pPlayer->score = 100;`
- 배열의 경우: `Player* arr = usr; arr[1].score = 50;`

### 2중 포인터
- 포인터 배열 또는 포인터 자체를 가리킬 때: `Player** ppPlayer`
- 사용 예시: `(*ppPlayer + i)->health += 10;`
- 주의: `*ppPlayer[i]`는 연산 우선순위 때문에 잘못됨, `(*ppPlayer)[i]` 또는 `(*ppPlayer + i)` 사용.
- &usr 같이 배열 전체 주소를 넘길 때 사용 가능하지만, 단일 포인터로도 충분히 처리 가능할 경우 굳이 2중 포인터를 쓰지 않아도 됨.

## 3. 오늘 문제 풀이에서 실수 및 헷갈렸던 부분

1. **2중 포인터 vs 단일 포인터**
- 원래 의도: `healAll`에서 Player**로 포인터 연습
- 실제 풀이: 단일 포인터 배열로 간단하게 구현 → 포인터 연산 헷갈림 최소화


2. **함수 매개변수와 인덱스**
- boostScore: 단순히 `usr[adplayer]`를 넘기고 싶었지만, 함수에서 인덱스 없이는 접근 불가
- 해결: 함수에서 인덱스 추가 `boostScore(Player* pPlayer, int idx, int amount)`


3. **함수 호출 시 & 사용 여부**
- `boostScore(&usr)` → 배열 전체 주소 전달, 함수 매개변수 설계 필요
- `healAll(usr)` → 단일 포인터 배열 전달, 함수 내 인덱스로 처리 가능
- 결론: &는 배열 전체 포인터 전달 시, 단일 포인터만 사용하면 충분할 때는 생략 가능


4. **포인터 연산 문법**
- 단일 포인터: `pPlayer[i].score`
- 이중 포인터: `(*ppPlayer + i)->health`
- `*ppPlayer[i]`는 잘못된 접근: `[]` 연산자가 `*ppPlayer[i]`보다 우선순위가 높음


5. **main 함수 루프 및 종료 문제**
- 초기 문제: 메뉴 선택 후 한 번 실행하면 종료
- 해결: `while(true)` 루프로 메뉴 반복, return 대신 continue 사용

## 4. 정리
- 구조체 배열 + 단일/이중 포인터 + 함수 매개변수 조합은 복잡함 → 작은 단위로 분리해서 구현
- 포인터 연산 순서와 괄호 사용법 중요
- &를 붙여 배열 전체 주소를 전달하는 경우와 단순 배열 이름으로 전달하는 경우 차이 이해
- 문제의 의도와 실제 풀이 경로가 다르더라도 핵심 개념(포인터, 구조체, 배열) 실습에 의미 있음
- 반복 실습으로 함수 전달 시 포인터 감각 강화 필요
