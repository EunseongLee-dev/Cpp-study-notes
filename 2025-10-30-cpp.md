# 📘 C++ 학습 정리 — 함수 포인터 & 문자열 분석 (2025-10-28)

## 🧩 1. 함수 포인터 (Function Pointer)

### 📖 개념 요약
- **함수의 주소를 저장하는 포인터 변수**.
- 함수를 변수처럼 전달하거나, 선택적으로 호출할 수 있음.
- 보통 다음 형태로 선언함:
  ```cpp
  반환형 (*포인터이름)(매개변수형식);
💡 예시
cpp
코드 복사
void Test()
{
    std::cout << "Hello Function Pointer!\n";
}

int main()
{
    void (*ptest)(void) = nullptr; // 함수 포인터 선언
    ptest = Test;                  // 함수 주소 저장
    ptest();                       // 포인터를 통해 함수 호출
}
🧠 핵심 개념
void(*ptest)(void)
→ 반환형이 void, 매개변수가 없는 함수의 주소를 받을 수 있는 포인터.
→ 괄호(())는 *가 포인터 변수명에 적용됨을 명확히 구분하기 위해 사용됨.

ptest = Test;
→ 함수 이름은 함수의 시작 주소를 의미하므로, &는 생략 가능.

ptest();
→ 함수를 직접 호출하듯 실행 가능.

⚙️ 활용 예시 (콜백 구조)
cpp
코드 복사
void sortASC(int* arr, int count);
void sortDESC(int* arr, int count);

void bubble(int* arr, int count, void(*func)(int*, int))
{
    func(arr, count); // 전달받은 함수 호출
}

int main()
{
    int data[5] = { 5, 2, 9, 1, 7 };
    bubble(data, 5, sortASC); // 오름차순 정렬
    bubble(data, 5, sortDESC); // 내림차순 정렬
}
➡️ bubble 함수는 직접 정렬 로직을 가지지 않고,
정렬 기능을 가진 함수의 “주소”만 전달받아 실행함.
즉, 중간 다리 역할을 수행.

🧩 2. 문자열 분석 문제 복습
📖 배운 문법
✅ getline()
공백 포함 문자열 입력:

cpp
코드 복사
std::getline(std::cin, 문자열변수);
✅ 범위 기반 for문 (range-based for)
문자열이나 배열의 각 요소를 자동으로 순회:

cpp
코드 복사
for (char c : 문자열)
{
    // 문자열의 각 문자 c를 순서대로 꺼냄
}
✅ 문자 비교 (아스키 코드 범위)
문자 범위	의미	아스키 코드
'a' ~ 'z'	소문자	97~122
'A' ~ 'Z'	대문자	65~90
'0' ~ '9'	숫자	48~57

💡 최종 예시 코드
cpp
코드 복사
#include <iostream>
#include <string>

int main()
{
    std::string nul;

    std::cout << "문자열 입력: ";
    std::getline(std::cin, nul);

    int countAlpha = 0, countNum = 0, countSpace = 0, countSpecial = 0;

    for (char c : nul)
    {
        if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
            countAlpha++;
        else if (c >= '0' && c <= '9')
            countNum++;
        else if (c == ' ')
            countSpace++;
        else
            countSpecial++;
    }

    std::cout << "알파벳: " << countAlpha << '\n';
    std::cout << "숫자: " << countNum << '\n';
    std::cout << "공백: " << countSpace << '\n';
    std::cout << "특수문자: " << countSpecial << '\n';

    return 0;
}
🧾 정리 메모
함수 포인터는 함수를 값처럼 다룰 수 있게 하는 도구.

괄호(())는 “포인터임을 명확히 표시”하기 위해 필요함.

for(char c : 문자열) 은 범위 기반 반복문 — 인덱스 없이 각 문자를 순회.

문자 비교는 아스키 코드 숫자 비교로 동작함.

📌 추가로 복습할 포인트
함수 포인터 매개변수 활용 (void(*func)(int*, int))

std::getline() vs std::cin 차이

for(auto c : 문자열) 형태로도 가능 — auto는 자료형 자동 추론.
