# 오늘 학습 정리: 문자열 & 포인터

---

## 1. 문자열 기초

- 문자열은 문자 배열로 표현 가능 (`char[]`, `wchar_t[]`, `char16_t[]`)
- 끝에는 항상 널 문자(`'\0'`)가 들어감
- 예시:
```cpp
char str[10] = "Hello";
wchar_t wstr[10] = L"안녕";
char16_t cstr[10] = u"안녕";
```
- 문자열 길이 계산 시, 널 문자를 제외하고 실제 문자 개수를 센다.

```cpp
int GetLength(const char* _str) {
    int i = 0;
    while (_str[i] != '\0') {
        ++i;
    }
    return i;
}
```

## 2. 포인터를 이용한 문자열 처리

- `_str[i]` == `*(_str + i)`
- `_str` 자체는 배열의 시작 주소, 즉 포인터와 같음
- 반복문으로 역순 출력:
```cpp
for (int i = len - 1; i >= 0; --i) {
    std::cout << *(_str + i);
}
```

## 3. `*`와 `()` 사용 차이

1. `*ptr` : 포인터가 가리키는 값에 접근
2. `ptr[i]` : `*(ptr + i)`와 동일
3. `(*ptr + i)` : 포인터가 가리키는 값에 i를 더함 (값 연산)
4. `*(ptr + i)` : i만큼 떨어진 메모리 위치에 접근

> 요약:
> - 괄호는 연산 우선순위 명확히 하기 위해 사용
> - 단일 포인터에서 배열 접근: `ptr[i]` 또는 `*(ptr + i)`
> - 2중 포인터에서 값 접근: `(*pp + i)->member`

## 4. 문자열 입력과 포인터

- `std::cin`은 `char[]`나 `std::string` 입력 가능
- `wchar_t` 또는 `char16_t`는 `std::wcin` 또는 `u16string` 활용 필요
- 예시:
```cpp
char log[100];
std::cin >> log;
```
- 역순 출력 시:
```cpp
for (int i = len-1; i >=0; --i)
    std::cout << log[i];
```

## 5. 문제풀이 시 주의점

- 비교 연산자 실수:
  - `i >= 0` : i가 0 이상일 때 반복
  - `i < size` : i가 size 미만일 때 반복
- 포인터 연산 혼동:
  - `*ptr + 1` : 포인터가 가리키는 값에 1 더함
  - `*(ptr + 1)` : 포인터가 가리키는 다음 주소의 값 접근
  - `(*ptr + 1)` : 괄호로 묶으면 연산 우선순위 명확

## 6. 헷갈렸던 포인터와 문자열 정리

- 단일 포인터 vs 2중 포인터:
  - 단일 포인터: `char* ptr` → `ptr[i]` / `*(ptr + i)`
  - 2중 포인터: `char** ptr` → `(*ptr)[i]` / `*(*ptr + i)`
- 함수 호출 시:
  - 배열 전체 주소를 넘길 때: `Player (*pp)[3]`
  - 배열 첫 번째 요소 주소만 넘길 때: `Player* pp`

---

## 7. 오늘 학습 느낀 점

- 문자열, 포인터 연산, 비교 연산자 헷갈림 주의
- 괄호 `()`와 `*` 위치에 따라 의미가 달라짐
- 반복문 조건과 배열 인덱스 범위 확인 필요
- 연습 문제를 통해 `char*`, `wchar_t*`, `char16_t*` 포인터 활용 감각 익히기

---

### 참고
- 반복문에서 비교 연산자는 항상 초기값과 종료 조건을 논리적으로 생각하며 작성
- 포인터 연산 시, `*`와 `()` 우선순위를 헷갈리지 않도록 주석 활용

