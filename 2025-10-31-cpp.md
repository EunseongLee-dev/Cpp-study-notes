# 🔗 연결 리스트 (Linked List) 구조 완전 정리

> **주제:** `Node`와 `Linklist` 구조의 관계, `Push` / `InitList` / `Rele` 동작 흐름 정리  
> **언어:** C++  
> **핵심 포인트:** 포인터 기반 연결, 구조체 간 관계, 주소 흐름 이해

---

## 🧱 1. 구조체의 구성

### `Node` 구조체
```cpp
struct Node
{
    int iData;           // 실제 데이터 (정수형)
    struct Node* nextnode; // 다음 노드의 주소를 저장하는 포인터
};
iData: 데이터를 저장하는 공간

nextnode: 다음 노드(Node)의 주소를 저장

즉, 하나의 Node는 자기 다음 노드로 향하는 “주소 화살표”를 가지고 있음.
```

### Linklist 구조체
```cpp
struct Linklist
{
    Node* headnode; // 연결 리스트의 시작점(첫 번째 노드의 주소)
    int icount;     // 현재 리스트에 들어있는 노드의 개수
};


headnode: 첫 번째 Node의 주소를 담고 있는 포인터

icount: 총 노드 개수를 관리하는 카운터

즉, Linklist는 “리스트 전체의 관리인” 같은 역할.
```

⚙️ 2. 함수별 역할

### ① InitList()
```cpp
void InitList(Linklist* _list)
{
    _list->headnode = nullptr;
    _list->icount = 0;
}
```

리스트 초기화 (아무것도 없는 상태로 시작)

headnode를 nullptr로 설정 → “비어 있다(empty)” 의미

icount는 0으로 초기화

### ② Push()
```cpp
void Push(Linklist* _list, int _idata)
{
    Node* pNode = new Node;    // 새 노드 생성
    pNode->iData = _idata;     // 데이터 저장
    pNode->nextnode = nullptr; // 다음 노드는 아직 없음

    if (_list->icount == 0)
    {
        // 리스트가 비어 있다면 첫 노드로 지정
        _list->headnode = pNode;
    }
    else
    {
        // 리스트가 비어 있지 않다면 마지막 노드를 찾아 새 노드 연결
        Node* pCur = _list->headnode;
        while (pCur->nextnode) // 다음 노드가 존재하는 동안 반복
        {
            pCur = pCur->nextnode;
        }
        pCur->nextnode = pNode; // 마지막 노드의 다음 주소에 새 노드 연결
    }

    ++_list->icount;
}
```

🔍 3. 동작 흐름 (단계별 시각화)

✅ 1차 Push (100 삽입)
```cpp
[100 | next → NULL]
↑
headnode (Linklist에서 관리)
```

✅ 2차 Push (200 삽입)
```cpp
[100 | next → ] → [200 | next → NULL]
↑
headnode
```

✅ 3차 Push (300 삽입)
```cpp
[100 | next → ] → [200 | next → ] → [300 | next → NULL]
↑
headnode
```

모든 노드는 자신의 다음 노드 주소를 nextnode에 저장함.
Linklist는 단 하나의 포인터(headnode)만으로 전체 구조를 관리.


### ③ Rele()
```cpp
void Rele(Linklist* _list)
{
    Node* pdata = _list->headnode;
    while (pdata)
    {
        Node* pnext = pdata->nextnode;
        delete pdata;
        pdata = pnext;
    }
}
```

메모리 해제용 함수

모든 노드를 순회하며 delete로 동적 메모리 해제

마지막까지 순회 후 리스트는 비워짐

### 💡 4. 자주 헷갈리는 부분 요약
```cpp
헷갈리는 개념	올바른 이해
Linklist vs Node	Linklist는 “리스트 전체”를 관리, Node는 “데이터 하나”를 저장
headnode의 의미	첫 번째 노드의 주소를 저장하는 포인터
nextnode	현재 노드가 “다음 노드의 주소”를 가리키는 포인터
Push에서의 연결	nextnode = pNode는 “다음 노드를 가리키는 화살표를 새 노드 주소로 바꾼다”는 뜻
nullptr 조건	연결의 끝(즉, 다음 노드가 없다)을 의미
Node* cur	현재 탐색 중인 노드의 주소를 저장하는 변수
```

### 🧠 5. 핵심 요약 문장
```cpp
Linklist는 “시작점(head)”을 관리하고

각 Node는 “데이터 + 다음 노드의 주소”를 가진다.

연결 리스트는 주소로 연결된 노드들의 사슬(chain) 구조이다.

nextnode = pNode;는 “다음 노드를 이 주소로 바꾼다”는 뜻이다.

따라서 headnode 하나만으로 모든 노드에 접근할 수 있다.
```

### 🧩 6. 전체 코드 정리
```cpp
// line.h
#pragma once

struct Node
{
    int iData;
    struct Node* nextnode;
};

struct Linklist
{
    Node* headnode;
    int icount;
};

void InitList(Linklist* _list);
void Push(Linklist* _list, int _idata);
void Rele(Linklist* _list);
```
```cpp
// line.cpp
#include "line.h"
#include <iostream>

void InitList(Linklist* _list)
{
    _list->headnode = nullptr;
    _list->icount = 0;
}

void Push(Linklist* _list, int _idata)
{
    Node* pNode = new Node;
    pNode->iData = _idata;
    pNode->nextnode = nullptr;

    if (_list->icount == 0)
    {
        _list->headnode = pNode;
    }
    else
    {
        Node* pCur = _list->headnode;
        while (pCur->nextnode)
        {
            pCur = pCur->nextnode;
        }
        pCur->nextnode = pNode;
    }

    ++_list->icount;
}

void Rele(Linklist* _list)
{
    Node* pdata = _list->headnode;
    while (pdata)
    {
        Node* pnext = pdata->nextnode;
        delete pdata;
        pdata = pnext;
    }
}
```
```cpp
// main.cpp
#include <iostream>
#include "line.h"

int main()
{
    Linklist list = {};

    InitList(&list);

    Push(&list, 100);
    Push(&list, 200);
    Push(&list, 300);

    Rele(&list);

    return 0;
}
```

### 🏁 결론
```cpp
연결 리스트는 "주소를 통해 데이터가 사슬처럼 이어지는 구조"이다.
Linklist는 시작점을 관리하고,
각 Node가 자신의 다음 노드를 주소로 가리키며 전체 구조를 형성한다.

headnode → [100] → [200] → [300] → NULL
```
