# C++ 함수 템플릿 / 클래스 템플릿 정리

## 🧩 함수 템플릿 (Function Template)

### ✅ 개념
- 동일한 기능을 여러 자료형에서 재사용하기 위해 **자료형을 매개변수화**한 함수.
- `T`는 **임시 자료형(템플릿 매개변수)**로, 컴파일 시 실제 자료형으로 치환됨.

### ✅ 기본 구조
```cpp
template <typename T>
T Add(T a, T b) {
    return a + b;
}
```

### ✅ 호출 예시
```cpp
cout << Add<int>(3, 5);       // 명시적 타입 지정
cout << Add(3.2, 4.1);        // 타입 추론 (컴파일러가 자동 판단)
```

### ⚠️ 주의할 점
- **템플릿은 컴파일 시점에 코드가 생성**되므로, 타입 불일치 시 에러가 발생함.
- **명시적 타입 지정**과 **타입 추론**이 다르게 동작할 수 있음.
- 함수 내부 연산(`+`, `-`, `==`, 등)은 해당 자료형에서 지원되어야 함.

---

## 🧱 클래스 템플릿 (Class Template)

### ✅ 개념
- 자료형에 따라 다른 클래스를 작성할 필요 없이 **템플릿으로 하나의 틀을 정의**.

### ✅ 기본 구조
```cpp
template <typename T>
class Calculator {
public:
    T Add(T a, T b) { return a + b; }
};
```

### ✅ 사용 예시
```cpp
Calculator<int> c1;
Calculator<double> c2;

cout << c1.Add(3, 5);
cout << c2.Add(1.1, 2.2);
```

### ⚠️ 클래스 템플릿 주의점
- 클래스의 **모든 멤버함수 정의가 헤더에 존재해야 함**  
  (템플릿 특성상, 소스 분리 시 링킹 오류 발생 가능)
- 템플릿 인스턴스화는 컴파일 시점에 일어남.

---

## 🧠 오늘의 정리 포인트

| 구분 | 내용 |
|------|------|
| 🔹 템플릿 핵심 | 자료형을 변수처럼 다루는 “일반화” 기법 |
| 🔹 함수 템플릿 | 동일 연산의 자료형만 다른 경우 효율적 |
| 🔹 클래스 템플릿 | 구조체/클래스에서 재사용성 강화 |
| 🔹 헤더 정의 이유 | 컴파일 타임 인스턴스화 때문에 cpp 분리가 어려움 |
| 🔹 혼용 문제 | int/double 등 서로 다른 타입은 자동 변환되지 않음 |
| 🔹 함수/클래스 관계 | 함수 템플릿은 기능, 클래스 템플릿은 구조 중심 |

---

## 💭 학습 중 헷갈렸던 부분 정리

1. **함수 템플릿은 오버로딩처럼 보이지만 다르다.**  
   → 템플릿은 컴파일러가 자료형별로 실제 함수를 ‘생성’함.

2. **클래스 템플릿은 왜 cpp 분리가 안 되지?**  
   → 템플릿은 사용 시점(컴파일 타임)에 코드가 만들어지므로, 정의가 헤더에 있어야 컴파일러가 인식 가능.

3. **서로 다른 자료형은 자동 변환 안 됨.**  
   → `Add(3, 3.14)`처럼 자료형이 다르면 명시적 형변환이 필요.

4. **템플릿은 실체가 없는 설계도이다.**  
   → 실제 코드가 되려면 ‘특정 타입’으로 인스턴스화돼야 함.

---

## 📝 오늘의 결론

> 템플릿은 “자료형에 구애받지 않는 재사용성”을 위한 문법이다.  
> 실제로는 컴파일 타임에 각각의 자료형에 맞는 함수나 클래스가 **자동 생성**되는 구조.  
> 즉, **‘작성은 한 번, 사용은 여러 번’**의 개념.

---

