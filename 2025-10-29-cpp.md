🌟 가변 배열 & 버블 정렬 정리
📘 1. 가변 배열 (Dynamic Array)
✅ 핵심 개념

정적 배열은 크기가 고정되어 있으나, 가변 배열은 필요에 따라 크기를 재조정 가능함.

C++에서는 new[]와 delete[]를 사용하여 동적 메모리 관리 수행.

✅ 주요 함수 정리
InitArr(tArr* _pArr)

배열 초기화

크기 2의 배열 생성

초기값 설정:

_pArr->aint = new int[2];
_pArr->icount = 0;
_pArr->iMaxcount = 2;

Reall(tArr* _pArr)

배열 공간 재할당 (2배 확장)

기존 데이터 복사 후 이전 공간 해제

int* pNew = new int[_pArr->iMaxcount * 2];
for (int i = 0; i < _pArr->icount; ++i)
    pNew[i] = _pArr->aint[i];

delete[] _pArr->aint;
_pArr->aint = pNew;
_pArr->iMaxcount *= 2;

Push(tArr* _pArr, int _iData)

공간이 꽉 찼을 경우 자동으로 Reall() 호출

if (_pArr->icount >= _pArr->iMaxcount)
    Reall(_pArr);

_pArr->aint[_pArr->icount++] = _iData;

Rele(tArr* _pArr)

동적 메모리 해제

delete[] _pArr->aint;
_pArr->icount = 0;
_pArr->iMaxcount = 0;

🎲 2. 랜덤 데이터 생성
✅ 핵심 포인트

C++11 이후에는 <random> 헤더 사용.

std::random_device + std::mt19937 + std::uniform_int_distribution 조합이 표준적 방식.

std::random_device rd;
std::mt19937 gen(rd());
std::uniform_int_distribution<int> dist(0, 99);

for (int i = 0; i < 10; ++i)
    Push(&s2, dist(gen));

🔄 3. 버블 정렬 (Bubble Sort)
✅ 알고리즘 요약

인접한 두 원소를 비교하여 큰 값을 뒤로 보냄.

한 바퀴(i) 돌 때마다 가장 큰 값이 끝으로 이동.

void bubble(tArr* _pArr)
{
    int temp;
    for (int i = 0; i < _pArr->icount; ++i)
    {
        for (int j = 0; j < _pArr->icount - i - 1; ++j)
        {
            if (_pArr->aint[j] > _pArr->aint[j + 1])
            {
                temp = _pArr->aint[j];
                _pArr->aint[j] = _pArr->aint[j + 1];
                _pArr->aint[j + 1] = temp;
            }
        }
    }
}

🧩 이해 포인트

icount - i - 1 → 이미 정렬된 마지막 요소 제외.

안쪽 for문이 완전히 끝나야 바깥 i 루프가 한 번 증가.

temp는 swap(교환) 임시 저장용 변수.

🧠 오늘 정리 포인트

new[] / delete[]는 쌍으로 사용해야 함.

🔹 pNew를 따로 delete하지 않아도 되는 이유
pNew는 단순히 새로 할당한 배열의 주소를 aint에 넘겨주기 위한 임시 포인터이기 때문에,
마지막에 aint를 delete[]하면 pNew가 가리키던 메모리도 함께 해제된다.
즉, pNew는 메모리를 “가리키는 역할”만 하고, 실제 메모리 해제는 aint가 담당한다.

구조체 포인터 전달 시 & 사용 (InitArr(&s1))

가변배열 확장은 항상 복사 → 해제 → 재연결 → 크기 업데이트 순서.

랜덤, 정렬 등 응용함수를 통해 구조체의 실사용 감각 익히기.
