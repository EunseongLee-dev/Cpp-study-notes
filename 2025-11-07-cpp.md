# 📘 C++ 템플릿 + 스택 구조 복습 정리

### 🧩 1. 템플릿 기본 구조 복습
```cpp
template <typename T>
class Stack {
private:
    Node<T>* top;
public:
    void push(const T& data);
    void pop();
    T peek() const;
    bool empty() const;
};
```

template <typename T> : 형(type)을 매개변수처럼 다루는 선언부.

클래스나 함수가 어떤 자료형이 들어올지 컴파일 시점에 결정되지 않음.

사용 예시:
```cpp
Stack<int> s1;
Stack<std::string> s2;
```

### 🧱 2. Node 구조체 생성자와 연결의 원리
```cpp
template <typename U>
struct Node {
    U data;
    Node<U>* _pPrev;

    Node(const U& data, Node<U>* prev = nullptr)
        : data(data), _pPrev(prev) {}
};
```

Node는 각 데이터와 이전 노드 주소를 가진다.

prev 기본값을 nullptr로 설정 → 첫 노드 생성 시 자동으로 비어있는 상태 유지.

### 🔄 3. push() 작동 원리
```cpp
void push(const U& data) {
    Node<U>* newnode = new Node<U>(data, top);
    top = newnode;
}
```
단계별 해석
```cpp
new Node<U>(data, top)
→ 새로운 노드를 생성하며, 이전 노드 포인터 _pPrev에 현재 top을 저장.

top = newnode;
→ 스택의 최상단(top)을 새 노드로 갱신.

예시
s1.push(10);
s1.push(20);
```

스택 호출 순서	newnode 생성 내용	top 변경 결과
```cpp
push(10)	(10, nullptr)	top → 10
push(20)	(20, 10의 주소)	top → 20 (20 → 10)
```

즉, top은 항상 최상단 노드를 가리키며
새 노드의 _pPrev가 이전 노드 주소를 기억하여 연결이 유지된다.

### ⚙️ 4. pop() 원리
```cpp
void pop() {
    if (top == nullptr) return; // 빈 스택 예외 방지
    Node<U>* temp = top;
    top = top->_pPrev;
    delete temp;
}
```

top을 한 단계 아래 노드로 이동.

기존 최상단 노드를 delete로 메모리 해제.


### 🧠 5. 오늘 헷갈렸던 핵심 포인트 요약
```cpp
🌀 템플릿 함수	U, T 혼용	템플릿 안에서도 <typename U>처럼 별도 선언 가능하지만, 일관성 유지 필요
🪜 push 내부 연결	newnode와 top 관계	newnode는 새 데이터, _pPrev는 이전 top의 주소 저장
🧩 top 갱신 순서	newnode → top 순서	항상 마지막에 top = newnode; 로 갱신해야 함
⚖️ if문 필요성	if (top == nullptr)	생성자 기본값으로 nullptr 처리하므로 불필요함
🧷 연결형 리스트와 차이	head vs top	리스트는 head부터 순차 탐색, 스택은 top만 사용하여 LIFO 구조 유지
```

### ✅ 6. assert 사용 관련
```cpp
assert(false) : 무조건 프로그램 중단 (명시적 오류 상황)

assert(nullptr) : 포인터가 null일 때만 중단

주로 **“프로그램 논리 검증용”**으로 사용한다.
```

### ✅ 7. 오늘 학습의 핵심 요약
```cpp
스택은 “마지막에 들어온 데이터가 먼저 나오는 구조 (LIFO)”.

push() : 새로운 노드를 만들어 top으로 교체.

pop() : top을 이전 노드로 되돌리며, 기존 노드 삭제.

템플릿은 타입 의존성을 제거하고 재사용성을 높인다.
```
